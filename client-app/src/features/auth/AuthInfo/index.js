import { useDispatch, useSelector } from "react-redux";
import { Link } from "react-router-dom";

import Button from "../../../components/Button";

export const AuthInfoPopup = () => {
  return (
    <div className="popup">
      <button className="popup"></button>
      <Button className="popup"></Button>
    </div>
  );
};
